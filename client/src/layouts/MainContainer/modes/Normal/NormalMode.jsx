/**
 * Normal Mode Component
 * Handles UI/logic for normal/established user mode
 * 
 * Features:
 * - Full access to all pages
 * - Swipe navigation enabled
 * - Nav clicks enabled
 * - Back button allows page navigation
 * - Full feature access
 */
export function NormalMode({ children }) {
  // Currently a pass-through wrapper
  // In the future, this can handle:
  // - Premium feature indicators
  // - Subscription status UI
  // - User preference overlays
  // - Advanced features UI
  
  return <>{children}</>;
}
