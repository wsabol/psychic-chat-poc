---
alwaysApply: true
---

On Windows/PowerShell, NEVER use grep or pipe with grep. Use PowerShell native commands instead. For docker-compose commands, just output the file directly without piping to search tools.